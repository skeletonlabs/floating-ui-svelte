import { createAttribute } from "./dom.js";

const FLOATING_ID_ATTRIBUTE = createAttribute("id");
const ORIGIN_ID_ATTRIBUTE = createAttribute("origin-id");

export { FLOATING_ID_ATTRIBUTE, ORIGIN_ID_ATTRIBUTE };
