{
  "name": "@skeletonlabs/floating-ui-svelte",
  "version": "0.3.2",
  "scripts": {
    "build": "vitest build",
    "build:watch": "vitest dev",
    "test": "vitest run",
    "test:watch": "vitest run --watch"
  }
}
